const formatOf = (descriptor) => {
  return `<span style='color: #748592;text-decoration: underline; text-decoration-color: #C8C8C8; font-weight: bold;'>${descriptor}</span>`;
};

export { formatOf };
